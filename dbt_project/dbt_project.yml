name: jobpulse
version: '1.0.0'
config-version: 2

profile: jobpulse

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  jobpulse:
    staging:
      +materialized: view
    gold:
      +materialized: table
